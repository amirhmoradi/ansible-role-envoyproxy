envoy_install_from_source: false

# Dependencies package state; use 'present' to make sure it's installed, or 'latest' if
# you want to upgrade or switch versions using a new repo.
dependency_packages_state: present

# Whether to install recommended packages. Used only for Debian/Ubuntu.
dependency_install_recommends: true
